// import PropTypes from 'prop-types';
import s from './Layout.module.css';

const Layout = () => {
    return <div className={s.Root}>Layout</div>;
};

Layout.propTypes = {};

Layout.defaultProps = {};

export default Layout;
