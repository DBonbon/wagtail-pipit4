// import PropTypes from 'prop-types';
import s from './Grid.module.css';

const Grid = () => {
    return <div className={s.Root}>Grid</div>;
};

Grid.propTypes = {};

Grid.defaultProps = {};

export default Grid;
