// import PropTypes from 'prop-types';
import s from './SearchIcon.module.css';

const SearchIcon = () => {
    return <div className={s.Root}>SearchIcon</div>;
};

SearchIcon.propTypes = {};

SearchIcon.defaultProps = {};

export default SearchIcon;
