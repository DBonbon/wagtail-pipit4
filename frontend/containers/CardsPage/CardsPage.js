// import PropTypes from 'prop-types';
import { basePageWrap } from '../BasePage';
import s from './CardsPage.module.css';

const CardsPage = () => {
    return <div className={s.Root}>CardsPage</div>;
};

export default basePageWrap(CardsPage);
